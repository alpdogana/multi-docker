sudo:required
services:
  - docker

before_install:
  - docker build -t dice/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true dice/react-test npm test

after_success:
  - docker build -t dice/multi-client ./client
  - docker build -t dice/multi-nginx ./nginx
  - docker build -t dice/multi-server ./server
  - docker build -t dice/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push dice/multi-client
  - docker push dice/multi-nginx
  - docker push dice/multi-server
  - docker push dice/multi-worker